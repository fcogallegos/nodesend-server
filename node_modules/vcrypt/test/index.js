const VCrypt = require("./index-m");
var test = VCrypt();
var testStr = "test lorem";
if(typeof test=="function"){
    test({bits:2048}, {bits:2048});
}
var ins = new VCrypt.Instance();
var a = ins.encrypt(testStr, VCrypt.Server);
console.log(ins.decrypt(a, VCrypt.Server).toString()==testStr?"All works correctly!":"Something is wrong...");
